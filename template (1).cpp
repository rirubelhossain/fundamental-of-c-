#include <bits/stdc++.h>
using namespace std;

template <class T>

void function( T a , T b)
{
   /// return(a + b );
    cout<<"The result is = "<<a+b<<endl;
}
int main()
{
    int a , b ;

    cin>>a>>b;
  ///  cout<<"The result will be = "<<function(a,b)<<endl;
    function(a,b);

    string x , y ;

    cin>>x>>y;
    function(x,y);

    ///cout<<"The string is = "<<function(x,y);

    return(0);
}
